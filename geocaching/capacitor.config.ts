import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.efecode.ionic',
  appName: 'ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
